/goal Investigate why the CI for git branch '{{.BUILD_URL}}' is red {{.WAIT_STATUS}}. Do NOT edit files, push, or trigger a build. Produce a written diagnosis only: the root cause of each failing job and the specific change you would make to turn it green, with the evidence (log lines, failing tests, exit codes) behind each. Do NOT read/reverse-engineer babystand.sh.{{.DEBUG_STRING}}
