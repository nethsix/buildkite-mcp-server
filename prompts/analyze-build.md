/goal Analyze the Buildkite build at {{.BUILD_URL}} (org '{{.ORG_SLUG}}', pipeline '{{.PIPELINE_SLUG}}') and propose changes to the pipeline that would make it build faster {{.WAIT_STATUS}}. Do NOT modify anything, push, or trigger a build — produce a written proposal only: concrete, ordered recommendations, each with the evidence behind it (step timings, bottlenecks, cache misses, serialized work that could parallelize). Do NOT read/reverse-engineer babystand.sh.{{.DEBUG_STRING}}
